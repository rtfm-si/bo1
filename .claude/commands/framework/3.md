<load_manifest path="audits/manifests/framework_pass3.manifest.xml" />

<manifest_reference>
  Use the loaded audit manifest to:
  - enforce scope
  - enforce constraints
  - ensure required_inputs are gathered
  - produce exactly the expected_outputs
  - respect activation_conditions
</manifest_reference>

<framework_request>
  <pass>3</pass>
  <name>Hardening Pass</name>

  <goal>
    Follow the purpose and scope defined in the manifest.
    Harden for safety, determinism, governance drift protection, multi-agent correctness.
  </goal>

  <constraints>
    - Follow manifest constraints.
    - Follow CLAUDE.md, GOVERNANCE.md, CONTEXT_BOUNDARY, MODEL_GUIDANCE, TASK_PATTERNS.
    - Do NOT regenerate from scratch.
    - Only HARDEN, SECURE, LOCK DOWN.
    - Keep additions short, atomic, operational.
  </constraints>

  <steps>
    Derive all steps from the manifest's <scope>, <required_inputs>, and <expected_outputs>.
  </steps>

  <output_format>
    - Diffs or very short summaries
    - Keep reasoning shallow and minimal
  </output_format>
</framework_request>
