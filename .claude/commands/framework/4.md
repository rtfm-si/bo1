<load_manifest path="audits/manifests/framework_pass4.manifest.xml" />

<manifest_reference>
  Use the loaded audit manifest to:
  - enforce scope
  - enforce constraints
  - ensure required_inputs are gathered
  - produce exactly the expected_outputs
  - respect activation_conditions
</manifest_reference>

<framework_request>
  <pass>4</pass>
  <name>Enforcement Layer Upgrade</name>

  <goal>
    Follow the purpose and scope defined in the manifest.
    Install mandatory pre-flight enforcement step for governance compliance.
  </goal>

  <constraints>
    - Follow manifest constraints.
    - Follow CLAUDE.md, GOVERNANCE.md, CONTEXT_BOUNDARY, MODEL_GUIDANCE, TASK_PATTERNS.
    - Do NOT regenerate files.
    - Only append enforcement layer.
    - Keep additions short, atomic, operational.
  </constraints>

  <steps>
    Derive all steps from the manifest's <scope>, <required_inputs>, and <expected_outputs>.
  </steps>

  <output_format>
    - Small diffs only
    - Very short summary of files modified
  </output_format>
</framework_request>
