<load_manifest path="audits/manifests/framework_pass5.manifest.xml" />

<manifest_reference>
  Use the loaded audit manifest to:
  - enforce scope
  - enforce constraints
  - ensure required_inputs are gathered
  - produce exactly the expected_outputs
  - respect activation_conditions
</manifest_reference>

<framework_request>
  <pass>5</pass>
  <name>Runtime Self-Audit & Policy Violation Guard</name>

  <goal>
    Follow the purpose and scope defined in the manifest.
    Add runtime self-auditing, policy-violation detection, hallucination prevention.
  </goal>

  <constraints>
    - Follow manifest constraints.
    - Follow CLAUDE.md, GOVERNANCE.md, CONTEXT_BOUNDARY, MODEL_GUIDANCE, TASK_PATTERNS.
    - Do NOT regenerate existing files.
    - Do NOT expand prior content.
    - Only add minimal, operational rules.
  </constraints>

  <steps>
    Derive all steps from the manifest's <scope>, <required_inputs>, and <expected_outputs>.
  </steps>

  <output_format>
    - Minimal diffs or short list of changed files
    - Keep reasoning shallow
  </output_format>
</framework_request>
