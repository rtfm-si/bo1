# =============================================================================
# Board of One - Local Development Environment Configuration
# =============================================================================
# This file documents the MINIMAL environment variables needed for local dev.
# Copy to .env and fill in your values.
#
# For production settings, see .env.example (full documentation)
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: LLM API Keys
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...
VOYAGE_API_KEY=...  # For embeddings (research cache, semantic similarity)

# -----------------------------------------------------------------------------
# REQUIRED: Database Credentials (generate with: openssl rand -hex 32)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=your_redis_password_hex32
POSTGRES_PASSWORD=your_postgres_password_hex32

# -----------------------------------------------------------------------------
# REQUIRED: SuperTokens Authentication
# -----------------------------------------------------------------------------
# API key for SuperTokens Core (generate with: openssl rand -hex 32)
SUPERTOKENS_API_KEY=your_supertokens_api_key_hex32

# SuperTokens Core URL - Docker hostname for container-to-container
SUPERTOKENS_CONNECTION_URI=http://supertokens:3567

# Domain settings for local development
SUPERTOKENS_API_DOMAIN=http://localhost:8000
SUPERTOKENS_WEBSITE_DOMAIN=http://localhost:5173

# Cookie settings for localhost (IMPORTANT: different from production)
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost

# -----------------------------------------------------------------------------
# REQUIRED: Google OAuth (for "Sign in with Google")
# -----------------------------------------------------------------------------
# Create at: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add Authorized JavaScript origins: http://localhost:5173
# 3. Add Authorized redirect URIs: http://localhost:8000/api/auth/callback/google
GOOGLE_OAUTH_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret

# Enable Google OAuth (usually the only provider for local dev)
GOOGLE_OAUTH_ENABLED=true
ENABLE_SUPERTOKENS_AUTH=true

# -----------------------------------------------------------------------------
# OPTIONAL: Admin & Debugging
# -----------------------------------------------------------------------------
# Admin API key for /api/admin/* endpoints
ADMIN_API_KEY=local_admin_key_change_in_production

# Development settings
DEBUG=true
LOG_LEVEL=DEBUG

# Closed beta mode (set to true to require email whitelist)
CLOSED_BETA_MODE=false

# -----------------------------------------------------------------------------
# OPTIONAL: Other OAuth Providers (disable if not testing)
# -----------------------------------------------------------------------------
LINKEDIN_OAUTH_ENABLED=false
GITHUB_OAUTH_ENABLED=false
TWITTER_OAUTH_ENABLED=false
BLUESKY_OAUTH_ENABLED=false

# =============================================================================
# Quick Start Checklist
# =============================================================================
# 1. Copy this file: cp .env.local.example .env
# 2. Generate secrets:
#    - REDIS_PASSWORD: openssl rand -hex 32
#    - POSTGRES_PASSWORD: openssl rand -hex 32
#    - SUPERTOKENS_API_KEY: openssl rand -hex 32
# 3. Set up Google OAuth:
#    - Go to https://console.cloud.google.com/apis/credentials
#    - Create OAuth 2.0 Client ID (Web application)
#    - Set JavaScript origin: http://localhost:5173
#    - Set redirect URI: http://localhost:8000/api/auth/callback/google
#    - Copy client ID and secret
# 4. Start services: make up
# 5. Verify auth: make auth-check
# 6. Open http://localhost:5173 and test login
#
# Troubleshooting: docs/runbooks/local-dev-auth.md
# =============================================================================
