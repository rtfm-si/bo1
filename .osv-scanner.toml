# OSV Scanner configuration
# https://google.github.io/osv-scanner/configuration/

# Vulnerability ignore entries (add only with documented justification)
# Format:
# [[IgnoredVulns]]
# id = "GHSA-xxxx-xxxx-xxxx"
# reason = "Brief justification why this is safe to ignore"
# expiry = "2025-06-01" # Optional: re-evaluate after this date

[[IgnoredVulns]]
id = "GHSA-6vgw-5pg2-w6jp"
# Low severity (CVSS 2.0) pip path traversal. We use uv with locked deps. Re-evaluate 2026-04-01
reason = "Low severity pip path traversal; uv locked deps mitigate risk"

[[IgnoredVulns]]
id = "GHSA-r6ph-v2qm-q3c2"
# cryptography 46.0.3 -> 46.0.5; pending uv lock update. Re-evaluate 2026-03-01
reason = "cryptography CVE-2026-26007; fix available in 46.0.5, pending dependency update"
