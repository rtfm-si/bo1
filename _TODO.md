confirm the admin page only visible and accessible (hide the button, protect the route) for admin authenticated users, and NOT for non-admin users?

consolidate completed tasks again in @\_TASK.md

quite often get an error 500 on accessing admin then using hard refresh
