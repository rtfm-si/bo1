<loader>
  <load_manifest path="{{MANIFEST_PATH}}" />
  <assert>Manifest loaded successfully</assert>
  <manifest_reference>
    Use the loaded audit manifest to:
    - enforce scope
    - enforce constraints
    - ensure required_inputs are gathered
    - produce exactly the expected_outputs
    - respect activation_conditions
  </manifest_reference>
</loader>
