# Framework Pass 4 - Enforcement Layer Manifest

<audit_manifest>
  <audit_type>framework_pass4</audit_type>

  <purpose>
    Install mandatory pre-flight enforcement step: governance compliance, model/token constraints, folder boundaries, context restrictions, agent safety, deterministic fallback.
  </purpose>

  <scope>
    - Add global pre-flight enforcement routine
    - Add enforcement mirrors to manifests and folder CLAUDE files
    - Add tag-level validation hooks
    - Add governance checkpoint block
    - Add agent integrity gate
    - Add model/token budget guard
  </scope>

  <constraints>
    - Do NOT regenerate files
    - Only append enforcement layer
    - Keep additions short, atomic, operational
    - No project logic modification
  </constraints>

  <required_inputs>
    - Root CLAUDE.md
    - All folder-level CLAUDE.md and manifest.md
    - TAGS.md
    - GOVERNANCE.md
    - MODEL_GUIDANCE.md
  </required_inputs>

  <expected_outputs>
    - <pre_flight_enforcement> section in CLAUDE.md
    - <local_enforcement> blocks in folder files
    - <enforcement_rules> in TAGS.md
    - <governance_checkpoint> in GOVERNANCE.md
    - <agent_integrity_gate> in GOVERNANCE.md
    - <enforcement_guard> in MODEL_GUIDANCE.md
  </expected_outputs>

  <activation_conditions>
    - After Pass 3 completion
    - Enforcement upgrade requirement
  </activation_conditions>

  <run_pattern>
    1. Add pre-flight to root CLAUDE.md
    2. Add local enforcement to folder files
    3. Add tag validation hooks
    4. Add governance checkpoint
    5. Add agent integrity gate
    6. Add model/token guard
    7. Output short summary
  </run_pattern>
</audit_manifest>
