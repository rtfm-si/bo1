# Framework Pass 5 - Runtime Self-Audit Manifest

<audit_manifest>
  <audit_type>framework_pass5</audit_type>

  <purpose>
    Add runtime self-auditing, policy-violation detection, output cross-checking, correctness veto mechanism, hallucination prevention.
  </purpose>

  <scope>
    - Global runtime self-audit in CLAUDE.md
    - Local self-audit hooks in folder CLAUDE files
    - Policy violation detector in GOVERNANCE.md
    - Task-level post-checks in TASK_PATTERNS.md
    - Hallucination-prevention hook
  </scope>

  <constraints>
    - Do NOT regenerate existing files
    - Do NOT expand prior content
    - Only add minimal, operational rules
    - Keep everything extremely concise
  </constraints>

  <required_inputs>
    - Root CLAUDE.md
    - All folder-level CLAUDE.md files
    - GOVERNANCE.md
    - TASK_PATTERNS.md
  </required_inputs>

  <expected_outputs>
    - <runtime_self_audit> in CLAUDE.md
    - <local_self_audit> in folder CLAUDE files
    - <policy_violation_detector> in GOVERNANCE.md
    - <post_task_checks> in TASK_PATTERNS.md
    - <hallucination_guard> in CLAUDE.md or GOVERNANCE.md
  </expected_outputs>

  <activation_conditions>
    - After Pass 4 completion
    - Final robustness upgrade
  </activation_conditions>

  <run_pattern>
    1. Add runtime self-audit to root CLAUDE.md
    2. Add local self-audit to folder files
    3. Add policy violation detector
    4. Add post-task checks
    5. Add hallucination guard
    6. Output minimal changes
  </run_pattern>
</audit_manifest>
