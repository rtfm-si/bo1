# Board of One Frontend Environment Variables

# API Base URL (backend API)
# BFF Pattern: All OAuth flows go through backend
PUBLIC_API_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

# Environment
PUBLIC_ENV=development

# NOTE: No Supabase or OAuth config needed in frontend anymore!
# All authentication is handled by backend via BFF pattern.
# Frontend only needs to know the backend URL.
